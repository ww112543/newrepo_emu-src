name: Branch build test

on:
  push:
    branches:
      - test-auto-commit
#   paths-ignore:
#     - '.github/**'

  workflow_dispatch:

jobs:
  build-msvc:
#   uses: ./.github/workflows/build-msvc-new.yml
    name: Test build
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: |
          echo "This should be call when rebased"